{
  "name": "@dominion/mobile",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "expo start --dev-client --host lan",
    "start:dev-client": "node ./scripts/start-dev-client.mjs",
    "android": "expo run:android",
    "ios": "npm run prebuild:ios && expo run:ios",
    "dev:check": "npm --prefix \"$(git rev-parse --show-toplevel)\" run dev:check",
    "prebuild:ios": "node ./scripts/check-mobile-deps.mjs && CI=1 expo prebuild --platform ios --clean",
    "ios:prebuild": "node ./scripts/check-mobile-deps.mjs && CI=1 expo prebuild --platform ios --clean",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ios:check": "node ./scripts/check-ios-toolchain.mjs",
    "start:expo-go": "expo start --host lan"
  },
  "dependencies": {
    "@dominion/shared": "0.0.1",
    "expo": "~55.0.0",
    "expo-dev-client": "~55.0.16",
    "event-target-shim": "^6.0.2",
    "react": "19.2.0",
    "react-native": "0.83.2",
    "react-native-webrtc": "^124.0.4",
    "react-native-safe-area-context": "~5.6.2"
  },
  "devDependencies": {
    "@types/react": "^19.2.0"
  }
}
